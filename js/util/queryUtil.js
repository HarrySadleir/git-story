class QueryUtil {

	/**
	 * Produces an array containing all contributors and their number of contributions over the given data.
	 * e.g. {name: CommitBot, contributions: 200}
	 */
	static getContributors(data) {
		// TODO: Implement
		return [];
	}

	/**
	 * Produces the given data stacked over the given time unit.
	 */
	static stackCommits(data, timeUnit) {
		// TODO: Implement
		return [];
	}

	/**
	 * Produces the file tree hierarchy at the given date.
	 */
	static fileTreeAtDate(data, date) {
		// TODO: Implement
		return [];
	}
}